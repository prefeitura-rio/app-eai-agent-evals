---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eai-agent
  annotations:
    secrets.infisical.com/auto-reload: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eai-agent
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: eai-agent
    spec:
      containers:
        - name: eai-agent
          image: ghcr.io/prefeitura-rio/app-agentic-search:stable
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: eai-agent-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
      restartPolicy: Always

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: eai-agent
spec:
  selector:
    app: eai-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eai-agent-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eai-agent-frontend
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: eai-agent-frontend
    spec:
      containers:
        - name: eai-agent-frontend
          image: ghcr.io/prefeitura-rio/app-agentic-search-frontend:stable
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          ports:
            - containerPort: 3000
          # Adicione recursos para garantir que o pod tenha CPU e memória suficientes
          # Isso pode ajudar a evitar lentidão devido a restrições de recursos
          resources:
            requests:
              cpu: "200m"  # Solicita 0.2 de um core de CPU
              memory: "256Mi" # Solicita 256 Megabytes de memória
            limits:
              cpu: "500m"  # Limita a 0.5 de um core de CPU (permite bursts)
              memory: "512Mi" # Limita a 512 Megabytes de memória
      restartPolicy: Always

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: eai-agent-frontend
spec:
  selector:
    app: eai-agent-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
